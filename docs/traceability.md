# Traceability matrix

Every FR and NFR in `INTERVIEWTREA-PHASE1-VIEWER.md` gets a row. Rows are added in the
same change that satisfies the requirement, not retrofitted at the end.

A row marked `Manual` has no automated test and says how it was verified instead.
Empty and honest beats populated and fabricated.

| Req ID | Requirement (short) | Design element | Test(s) | Status |
|---|---|---|---|---|
| NFR-301 | No DICOM/rendering/geometry code references `System.Windows.*` | `Directory.Build.props` — all projects target plain `net8.0`, so WPF types are absent from the compilation. `App` (It. 2) is the only project permitted `net8.0-windows`. | Manual — confirmed `System.Windows.Point` fails to resolve in `InterviewTrea.Core` (CS0246). Note the `System.Windows` *namespace* root does exist in net8.0 via `System.Windows.Input.ICommand`; it is the WPF types that are unavailable. | Done |
| NFR-303 | CI builds and runs all tests on every push to `main` and every PR | `.github/workflows/ci.yml` — restore, `build -warnaserror`, `test` on `windows-latest`, SDK from `global.json` | Manual — the pipeline is its own evidence | Done |
| DQ-1 | Test data shall not be committed; `data/` gitignored | `.gitignore` | Manual — `data/` entry present | Done |
| NFR-101 | 512x512x400 volume under 300 MB managed heap | `Volume` — flat `short[]` of Hounsfield units, x fastest; `short` over `float` is what buys the budget | `AFullChestVolume_FitsTheMemoryBudget` — allocates the real array and asserts 209,715,200 bytes | Done |
| FR-206 (groundwork) | Oblique reslicing shall sample the volume by trilinear interpolation | `Volume.SampleTrilinear` / `SampleNearest`, in continuous voxel coordinates with patient-space overloads. Out of bounds returns `Volume.OutsideValue` (-1024) rather than throwing, because every oblique frame has corners outside the data and the sampler is a hot path. | `VolumeSamplingTests` — analytic expectations throughout: a 100 HU/voxel ramp reads exactly 50 at the midpoint; an `i + 10j + 100k` ramp pins all three axes independently. Mutation-verified against a swapped y/z stride, an inverted lerp weight, a half-voxel offset, and a missing far-face clamp. | Partial — the reslicer that consumes this arrives in Iteration 3 |
| FR-101 | Load a CT series from a user-selected directory | `SeriesLoader.Scan` — recurses the tree, parses headers only (`FileReadOption.SkipLargeTags`), skips unreadable files with a stated reason | `Scan_FindsASeriesAndItsGeometry`, `Scan_SkipsNonDicomFilesAndSaysWhy`, `Scan_OnAMissingDirectory_Throws` | Done |
| FR-102 | Group files by SeriesInstanceUID and prompt when several are present | `SeriesLoader.Scan` returns every candidate `SeriesDescriptor`, largest first. There is no UI in Iteration 1, so the loader offers and the caller chooses; the prompt itself is Iteration 2. | `Scan_RecursesAndGroupsBySeriesInstanceUid` | Partial — grouping done, prompt deferred to It. 2 |
| DI-3 | Tolerate missing or empty optional tags without throwing | `SeriesLoader.ReadMetadata` — every descriptive tag read with a default; `VolumeMetadata` makes the distinction structural by marking only the UIDs and modality `required` | `Scan_ToleratesAbsentOptionalTags`, `OmitOptionalTags_LeavesTheRequiredOnesIntact` | Done |
| (new) DQ-4 | Unreadable and non-DICOM files shall be skipped with a stated reason, not fail the load | `SeriesLoader` returns `SkippedFile(path, reason)` for each | `Scan_SkipsNonDicomFilesAndSaysWhy` | Done — requirement proposed in the Iteration 1 plan, not yet in the spec |
| (new) DQ-5 | Enhanced/multi-frame CT shall be rejected explicitly rather than mis-parsed | `SeriesLoader.TryReadHeader` skips any file with NumberOfFrames > 1 | None yet — the synthetic builder writes single-frame only | Partial — requirement proposed in the plan, not yet in the spec |
| FR-103 | Sort slices by projection of ImagePositionPatient onto the slice normal, not by InstanceNumber | `GeometryValidator.Validate` — ordering and validation are one step, because every uniformity check is a statement about successive slices | `SortsByPositionAndIgnoresInstanceNumber` — input reversed and InstanceNumbers reversed with it | Done |
| FR-105 | Reject a series whose slices do not share a FrameOfReferenceUID | `GeometryValidator.RequireSingleFrameOfReference` | `MismatchedFrameOfReference_IsRejected` | Done |
| FR-106 | Reject non-uniform slice spacing exceeding 1% variance | `GeometryValidator.RequireUniformSpacing` — variance read as (max gap − min gap) / median gap, the spec leaving the quantity undefined. Distinct messages for a duplicated slice, a missing slice, and merely uneven spacing. Tolerance is a constructor parameter. | `AMissingSlice_IsRejectedWithItsOwnMessage`, `DuplicateSlicePositions_AreRejectedWithTheirOwnMessage`, `MerelyUnevenSpacing_IsRejectedWithoutBeingMisdiagnosed`, `SpacingWithinTolerance_IsAccepted` | Done |
| FR-107a | Reject a malformed ImageOrientationPatient (direction cosines not perpendicular) | `GeometryValidator.Validate` — \|row · column\| > 1e-6 | `SkewedOrientation_IsRejectedAsMalformed` | Done — replaces the spec's FR-107, which tested orthogonality and so could never fire |
| FR-107b | Reject a series acquired with gantry tilt | `GeometryValidator.RequireNoGantryTilt` — the slices must stack along their own normal: \|d · n\| ≥ 1 − 1e-3 where d is the measured step direction. Tolerance is a constructor parameter and wants tuning against a real tilted series. | `GantryTilt_IsRejectedEvenThoughOrientationIsOrthogonal`, `TiltWithinTolerance_IsAccepted`, `ATighterTiltTolerance_RejectsWhatTheDefaultAccepts` | Done |
| FR-104 | Convert stored pixel values to Hounsfield units using RescaleSlope and RescaleIntercept | `PixelDecoder.DecodeInto` — honours PixelRepresentation, BitsStored and HighBit, decodes into a caller-owned span, saturates rather than wrapping at the `short` bounds and counts how often | `UnsignedPixels_AreRescaledToHounsfield`, `SignedPixels_AreRescaledToHounsfield`, `BitsStoredNarrowerThanBitsAllocated_MasksThePadding`, `SignedNarrowPixels_SignExtendFromBitsStored`, `ANonUnitSlopeIsApplied`, `ValuesBeyondShortRange_SaturateAndAreCounted` | Done |
| NFR-102 | Peak memory during load shall not exceed 2x steady-state volume size | `VolumeBuilder.Build` allocates the flat `short[]` once from the header dimensions and decodes each slice directly into its span; `SeriesLoader` parses headers with `SkipLargeTags` so a scan never materialises pixel data | Manual — profiled on real series. Peak working set above a 77.7 MB runtime baseline: **1.01x** the volume at 66.5 MB (LIDC-IDRI-0001), **2.03x** at 228 MB (LIDC-IDRI-0599, and 1.82x on a repeat run). The 1.01x figure proves there is no second live copy; the spread at 228 MB is transient per-slice pixel buffers outrunning gen-0 collection, not a copy of the volume. | Partial — the bound is met on the smaller series and sits at or just over 2x on the larger one |
| FR-109 | Load a 400-slice 512x512 series in under 15 seconds on a mid-range laptop | `SeriesLoader` parses headers only during the scan; `VolumeBuilder` decodes each slice once, directly into the final array | Manual — LIDC-IDRI-0599, 456 slices at 512x512 (228 MB): **1.6 s warm, 3.4 s cold**, measured three times, against a 15 s target. Cold includes process start and a cache-miss read of 160 MB from disk. Machine as in `docs/performance.md`. | Done |
| DQ-2 | Document which collection and series UIDs were used, so the project is reproducible | `docs/data-setup.md` | Manual — two LIDC-IDRI series named by UID, with the single curl that retrieves each | Done |
| §10 | Done when the probe prints the load summary for a real TCIA series and air reads about -1000 | `tools/InterviewTrea.Probe` — owns the Generic Host and registers the loader, validator and builder | Manual — run against LIDC-IDRI-0001: `Loaded 133 slices, 512x512x133, spacing 0.7x0.7x2.5 mm, HU range -2048..3071` and `Air reads about -1000 HU`. The rejection path is verified against synthetic series, since a deliberately broken one cannot be downloaded. | Done |
| RQ-2 | The README shall carry the regulatory statement above the fold | `README.md` — first line under the title, before any prose | Manual | Done |
| RQ-4 | The README shall state that this is independent study with no vendor affiliation | `README.md` — second block, above the fold | Manual | Done |
| §8.3 | The README shall state that coverage targets the domain and rendering layers and leaves the view layer to manual verification | `README.md` — Testing section | Manual | Done |
| FR-305 | Window/level presets: Lung, Soft Tissue, Bone, Brain, Mediastinum | `WindowLevel` static presets; `WindowLevelLut` precomputes the Hounsfield-to-grey table once per window change rather than per pixel, spanning the full `short` range so the render loop needs no bounds test | `ThePresetsMatchTheRequirement`, `TheLungPresetSeparatesAirFromSoftTissue`, `ANarrowWindowClipsHarder` | Done |
| FR-306 | Apply the series' own WindowCenter/WindowWidth as the initial preset when present | `WindowLevel.InitialFor` — falls back to Soft Tissue when absent or when the width is unusable | `TheSeriesOwnWindowWinsWhenItHasOne`, `WithoutAUsableSeriesWindowTheInitialPresetIsSoftTissue` | Done |
| FR-202 | Each viewport shall render a plane resampled from the reconstructed volume | `ResliceRenderer.RenderAxial` — an axial slice is one contiguous run in the flat array, so the render is a walk of that run through the window/level table with no interpolation; sample points land exactly on voxel centres. Coronal, sagittal and oblique planes arrive in Iteration 3. | `RenderAxial_WritesRowMajorWithXFastest`, `RenderAxial_SelectsTheRequestedSlice`, `RenderAxial_ClipsRatherThanWrapsOutsideTheWindow`, `RenderAxial_AppliesTheCurrentWindow` | Partial — axial only |
| RQ-1 | Persistent non-dismissible regulatory banner in the application | `MainWindow.xaml` — a fixed row of the root grid with no close affordance and nothing that can collapse it; colours from `Themes/Dark.xaml` | Manual — view layer, per spec 8.3 | Done |
| FR-108 | Determinate progress indicator during load; the application stays responsive | `SeriesLoader.Scan` and `VolumeBuilder.Build` take an optional `IProgress<double>`; `MainViewModel.LoadAsync` runs both on a thread-pool thread and maps them onto one bar, giving the header scan 15% of it because pixel decoding dominates the cost | Manual — view layer, per spec 8.3. The progress plumbing below the view is exercised by every existing loader and builder test through the default null argument. | Done |
| FR-301 | Mouse wheel over a viewport scrolls slices along that viewport's normal | `MainViewModel.ScrollSlices` clamps to the stack; `MainWindow.OnViewportMouseWheel` converts wheel delta to notches | Manual — verified on the synthetic phantom: ten notches moved slice 31 to 41 and stopped at both ends | Done — axial only |
| FR-302 | Right-drag adjusts window width (horizontal) and window level (vertical) | `WindowLevel.AdjustedBy`; sensitivity in Hounsfield units per pixel is a named constant in `MainWindow`, a calibration knob rather than a derived value | `AdjustingMovesBothAxesIndependently`, `TheWidthIsFlooredAtOneRatherThanInverting`; manual — a 150x90 px drag moved W 400 to 1000 and L 40 to 220, matching the constants exactly | Done |
| FR-303 | Middle-drag pans; Ctrl+wheel zooms about the cursor | `MatrixTransform` on the slice image; `Matrix.ScaleAt` about the cursor in screen space, so anatomy under the pointer stays put | Manual — view layer, per spec 8.3 | Done |
| FR-208 (in-plane) | Anisotropic voxels shall not distort the displayed image | The `WriteableBitmap` is created with DPI 25.4 / spacing-mm on each axis, so WPF's natural size for the image is proportional to its physical extent and layout needs no knowledge of millimetres | Manual — the coronal and sagittal aspect cases arrive with Iteration 3 | Partial — in-plane only |

## Iteration 3

| Req ID | Requirement (short) | Design element | Test(s) | Status |
|---|---|---|---|---|
| FR-202 | Each viewport shall render a plane resampled from the volume | `PlaneRenderer.Render` — one path for every orientation. Supersedes the axial-only note above; `ResliceRenderer.RenderAxial` is retained as the NFR-201 fast-path comparison, not as the UI's path. | `OnANativeGridTheGeneralPathMatchesTheAxialFastPath` ties the general path byte-for-byte to the already mutation-tested axial one | Done |
| FR-206 | Oblique planes shall be rendered by trilinear interpolation | `ReslicePlane` (origin plus two orthogonal step vectors) walked by `PlaneRenderer.Render`. The step vectors are converted to voxel space once per frame via `Matrix4x4Affine.TransformDirection`, so the inner loop is three adds, a trilinear fetch and a table lookup — no matrix multiply per pixel and no trigonometry. Rotated planes need no new code path, which is what FR-307 depends on. | `SamplingBetweenVoxelsInterpolatesRatherThanRepeating` (8 HU/voxel ramp on a half-millimetre grid: every odd column is a midpoint and must read the mean), `HalfUnitSamplesRoundRatherThanTruncate`, `SamplesOffTheEndOfTheVolumeReadAsOutside` | Done |
| FR-208 | Aspect ratio shall be correct for anisotropic voxels | The output grid is isotropic **in patient space**: both of a plane's step vectors are the same length in millimetres, so a 0.7 x 0.7 x 3.0 mm volume resliced coronally produces ~134 rows of genuinely interpolated data rather than 32 stretched ones. Supersedes the Iteration 2 bitmap-DPI approach, which could correct the proportions but could not create the missing rows. | `ASphereStaysRoundInACoronalReslice` — the two great-circle diameters must agree within the grid's own quantisation, against the 4.3x error a missing correction costs; `CoronalResolvesTheSliceAxisInMillimetresRatherThanInSlices`; `AnObliquelyAcquiredVolumeGetsAGridWideEnoughForItsFootprint` | Done || FR-207 | The slab viewport shall support Maximum, Minimum and Average projection over a user-set thickness | `SlabRenderer.Render` with `SlabMode`. The in-plane walk is `PlaneRenderer`'s; the addition is an inner loop along the normal, centred on the plane so thickness grows outwards and the anatomy under the crosshair stays put. Samples along the normal are spaced at the plane's own pixel pitch. Out-of-bounds samples are skipped rather than folded in as -1024 — MIP would survive that, MinIP would report solid air along every edge. | `EachModeCollapsesTheSlabItsOwnWay` (a ramp read along its gradient gives the three modes three separately derived answers), `AThickerSlabReachesFurtherUpTheRamp`, `ASlabThinnerThanOneSampleIsThePlaneItself`, `SamplesOutsideTheVolumeAreSkippedRatherThanCountedAsAir`, `TheDepthPitchFollowsTheGridRatherThanBeingFixed` | Done — renderer; the viewport and its controls follow in T18/T19 || NFR-201 | Axis-aligned slice render under 8 ms at 512x512 | `ResliceRenderer.RenderAxial` (0.105 ms) and `PlaneRenderer.Render` on an axial plane (2.354 ms). The viewports use the general path; the fast path is kept as the measured comparison. | `ResliceBenchmarks.AxialFastPath`, `AxialThroughThePlaneRenderer` — see `docs/performance.md` | Done — 3.4x headroom on the path actually used |
| NFR-202 | Oblique reslice under 16 ms at 512x512 | `PlaneRenderer.Render` on a plane 30 degrees out of axial (1.949 ms) | `ResliceBenchmarks.ObliqueReslice` | Done — but see the note in `docs/performance.md`: part of that plane falls outside the volume, so the figure flatters the oblique path |
| NFR-203 | Slab MIP over 20 mm under 33 ms | `SlabRenderer.Render`, 30 samples per output pixel, rows rendered on `Parallel.For` | `ResliceBenchmarks.SlabMaximum20Mm`, `SlabAverage20Mm` | Done — 14.1 ms, 2.3x headroom. Baseline was 81.5 ms and missed the target; before and after figures, and the one optimization that was tried and reverted for making it 43% slower, are in `docs/performance.md` |
| NFR-304 | Every NFR-200 target has a committed BenchmarkDotNet result with before/after figures | `tests/InterviewTrea.Benchmarks`, results transcribed into `docs/performance.md` with the machine, the workload and an honest reading of each row | Manual — the benchmark project is the artefact; it is excluded from CI because a two-minute run on shared hardware produces meaningless numbers | Done — baseline captured || FR-201 | Four viewports in a 2x2 layout: axial, coronal, sagittal, slab | `MainWindow.xaml`'s viewport grid holding four `ViewportControl`s, each bound to one `ViewportViewModel` and to the shared `MainViewModel` | Manual — see `docs/images/mpr-2x2.png`, captured from the running application against LIDC-IDRI-0599 (456 slices, 0.56 x 0.56 x 0.70 mm) | Done |
| FR-203 | Single-viewport maximized mode, toggled by double-click | `MainViewModel.ToggleMaximized` and `MainWindow.ApplyLayout`. The maximized pane is stretched across all four cells rather than re-parented, so it keeps its bitmap, its zoom and its handlers | Manual — verified by simulated double-click; the pane refits and its readouts survive | Done |
| FR-204 | Orientation overlay (A/P/L/R/S/I at the edges) | `AnatomicalDirection.Of` applied to each display axis in `ViewportViewModel.SetMarkers`, called on every update rather than once in the constructor — FR-307 turns the axes under the markers, and a frozen letter can end up 180 degrees wrong, which is the wrong-side error the markers exist to prevent. Four `TextBlock`s pinned outside the pane's transform | `AnatomicalDirectionTests.TheStandardPlanesCarryTheExpectedMarkers`; oblique behaviour manual, and the frozen-marker defect was found only by screenshotting a rotated frame | Done |
| FR-205 | Slice position in mm, window/level, zoom factor; no patient identifiers (DI-1) | `ViewportViewModel.PositionLabel` quotes the position on the patient axis the plane cuts, `MainViewModel.WindowLabel` the window, and the control's own `ZoomLabel` the zoom, where 1.00 is fit-to-pane. Nothing from `VolumeMetadata` reaches the overlay | Manual — visible in `docs/images/mpr-2x2.png` | Done |
| FR-208 | Aspect ratio correct for anisotropic voxels — view layer | Bitmaps are created at 96 dpi and the fit transform is uniform on both axes. The Iteration 2 DPI trick is gone: the grid is isotropic in millimetres, so any non-default dpi would re-introduce the distortion it used to remove | Manual — the coronal pane of the 0.7 x 0.7 x 3.0 mm phantom is proportionate | Done — supersedes the Iteration 2 partial row |
| FR-301 | Wheel scroll moves through slices | `MainViewModel.ScrollAlongNormal`. The step is the volume's own spacing along that pane's normal, so a notch advances to the next plane of real data — 3 mm axially and 0.7 mm sagittally on a chest series | Manual | Done |
| FR-304 | Clicking a point in one viewport moves the other two to intersect it | `MainViewModel.SetCrosshair`. The crosshair is a patient-space `Point3D`, not a slice index, so linking is a consequence of the model rather than a feature: no code knows which pane a click came from. `ViewportControl` inverts its own view matrix to turn a mouse position into that point | Manual — a simulated click in the axial pane moved the coronal readout from y -35.6 to -54.7 mm and the sagittal from x -55.6 to -70.8 mm, and both images changed to the new planes | Done |
| FR-207 | Slab thickness user-set, 1 to 100 mm | `MainViewModel.AdjustSlabThickness`, geometric at 1.2x per notch so the hundredfold range crosses in about twenty-five notches. Bound to Shift+wheel over the slab pane; the mode is the toolbar dropdown | Manual — five notches took 20.0 mm to 49.8 mm, which is 20 x 1.2^5 | Done — viewport and controls |

## Iteration 4

| ID | Requirement | Where it is satisfied | Evidence | Status |
|---|---|---|---|---|
| FR-307 | Oblique reslicing: dragging a crosshair arm rotates the reslice plane, dependent viewports update live | Three parts. `Vector3D.RotatedAbout` is Rodrigues' formula, written out rather than assembled as a matrix because the frame stores its axes and turns them in place, so drags compose in the stored state. `ReslicePlane.Through` gained an overload taking the two axes rather than looking them up, and the enum overload now delegates to it so the oblique and standard paths cannot drift apart; the existing corner-projection sizes a tilted footprint by the same arithmetic. `MainViewModel` holds one row/column axis pair per orientation and `RotateAbout` turns the two that are not the dragged pane's, both by the same angle about its normal. `ViewportControl` hit-tests the arms, converts pointer angle to rotation, and draws each crosshair line along `P.Normal x Q.Normal` | `Vector3DRotationTests` (12, including handedness, composition, and a 30-degree case whose expected value is irrational so it cannot be met by arithmetic exact only at quarter turns); `ReslicePlaneTests.TheAxesOverloadReproducesTheStandardPlane`, `AQuarterTurnWithinThePlaneSwapsWidthAndHeight`, `TiltingTheAxesTiltsTheNormalByTheSameRotation`, `ATiltedPlaneCoversTheLongerChordThroughTheVolume`, `TheIntersectionOfTwoPlanesLiesInBothOfThem`, `RotatingAPlaneTurnsItsCrosshairArmByTheSameAngle`, `TurningTheOtherTwoPlanesKeepsTheTriadOrthogonal`. Interaction verified manually against LIDC-IDRI-0001: a 36-degree drag on the axial pane's sagittal arm left the axial image untouched, turned both arms by 36 degrees, and re-cut the coronal and sagittal panes | Done |
| FR-401 | Linear distance between two points, reported in mm | `Measurement.LengthMillimetres` — the two endpoints are stored in patient millimetres, so a length is a subtraction and there is no scale factor left to forget | `DistanceIsTheStraightLineThroughPatientSpace` (a 3-4-12 Pythagorean quadruple, exactly 13 mm, so a length computed on one axis and broadcast cannot pass) | Done — drawn on the plane, with the length printed beside it to one decimal |
| FR-402 | Distance computed in patient space, correct for anisotropic spacing and oblique planes | Storing `Start`/`End` as `Point3D` in patient space is the whole of it: spacing and obliquity are already inside the coordinates by the time they arrive. A pixel-space measurement would need the pane's pitch, the plane's axes and the voxel spacing reapplied on read | `DistanceDoesNotDependOnTheFrameItWasDrawnOn` | Done |
| FR-403 | Elliptical ROI reporting area, mean HU, SD, min and max | Area in closed form from the two spans — pi/4 of the dragged box, not pi times the spans, which is the factor-of-four error that looks plausible because the outline is drawn from the same two numbers. Statistics by `RoiStatistics.Compute`, which walks the region on its own frame and samples **nearest-neighbour**: interpolation pulls each sample towards its neighbours and so deflates the one statistic whose purpose is to report spread. SD divides by n — the voxels in the region are the whole population, not a sample of one. Out-of-bounds samples are skipped, not folded in as -1024 | `AnEllipseEnclosesPiOverFourOfItsBoundingBox`, `TheBoxCornerIsInsideTheRectangleAndOutsideTheEllipse`, `TheEllipseBoundaryIsAtTheEndOfTheSemiAxis`, `TheEllipseDropsTheCornersTheRectangleKeeps`; `ARampReportsItsMidpointAndItsAnalyticSpread` (population variance of an arithmetic sequence, d^2(n^2-1)/12 = 52500, derived on paper); `SamplesOutsideTheVolumeAreSkippedRatherThanCountedAsAir`; `ACoarsePitchStepsOverAThinStructure` | Done — outline, area and the four statistics printed beside the region |
| FR-404 | Rectangular ROI with the same statistics | Same `Measurement` record, two switch arms. `Math.Abs` on the area because a drag up-and-left has two negative spans whose signs cancel and one negative span that does not | `ARectangleEnclosesTheProductOfItsSpans`, `AreaIsUnsignedWhicheverWayTheDragWent`, `APointBeyondAnyEdgeIsOutsideAReversedRectangle`, `AUniformRegionHasNoSpread`, `ARegionThinnerThanThePitchStillReportsAVoxel` | Done — as FR-403 |
| FR-406 | Measurements persist while scrolling; hidden when the slice is more than half a slice thickness away | `Measurement.IsVisibleOn(plane, tolerance)`. The spec's "half a slice thickness" has no meaning once FR-307 exists — an oblique plane has no slices — so it is read as distance with the tolerance handed in by the caller, the only place that knows the volume's spacing. The parallelism test is an addition, not in the spec, and is not optional: every measurement sits on the crosshair and the crosshair is on all three planes at once, so distance alone would draw an axial ROI edge-on in the sagittal pane | `AMeasurementIsVisibleOnThePlaneItWasDrawnOn`, `AMeasurementFurtherAwayThanTheToleranceIsHidden`, `AnAxialMeasurementDoesNotAppearInTheSagittalPaneDespiteTouchingIt`, `AFrameFacingTheOppositeWayIsStillTheSamePlane`, `ARotatedPlaneNoLongerCarriesTheMeasurement` | Done — the adorner layer applies the test on every redraw, with the tolerance set to half `MainViewModel.StepAlong(plane.Normal)`, which is half a wheel notch |
| FR-405 | Live Hounsfield readout under the pointer | `ViewportControl.HounsfieldUnder`, bound to a monospace overlay at the top right of each pane. Nearest-neighbour, deliberately: this is the number a user reads off a pixel and then expects inside the mean of an ROI drawn round it, so it has to be sampled the way `RoiStatistics` samples. Empty on the slab pane, where the displayed pixel is a projection through a thickness and no single voxel is under the cursor, and empty off the end of the data rather than showing `Volume.OutsideValue` | Manual — the readout tracks air at about -1000 and bone in the high hundreds, and matches the mean of a small ROI drawn over uniform tissue | Done |
| FR-401/403/404 drawing | The gesture that creates a measurement | `ViewportControl.TryStartMeasurement`. A selected tool takes the left button over from navigation completely — no arm grab, no crosshair move — because one press cannot mean two things and a drawing gesture that scrolled the slice out from under the drawing would be unusable. The frame is anchored at the pressed point, whose axes are the pane's current ones, so an oblique drag records the plane it was made on. A drag shorter than one output pixel is discarded as a mis-click. Outlines are drawn inside the pane's view transform in plane pixels, so they follow zoom and pan with no second copy of the geometry | Manual | Done — outlines and readouts; CSV export to follow |
| FR-407 | Delete an individual measurement and clear all | Hovering an outline sets `MainViewModel.Hovered` and draws it at double weight, which is the whole affordance — no selection colour, no mode. Delete is handled on the window rather than on a pane, so no viewport has to hold keyboard focus and focus does not have to follow the mouse. The pointer catches a region by its transparent interior and a distance by an invisible eight-pixel line under the visible one, because a 1.5-pixel outline is too fine to point at. Clear is a toolbar button; a new series clears the list too, since those coordinates belong to the previous frame of reference | Manual | Done |
| FR-310 | Reset: anatomical planes, crosshair at the centre of the volume, measurements cleared | `MainViewModel.Reset`. Not in the original spec - added at the user's request during Iteration 4 review, because rotation is easy to get lost in and there was no way back. Zoom and pan are deliberately excluded: they are per-pane state in the view's own matrix, and undoing a magnification the user set up is more than the button says it does. `AxesVersion` is bumped after the crosshair, so a pane whose own plane did not move still redraws the arms of the two that did | Manual | Done |
| FR-306 (view) | The dropdown names the window that is on screen | The retemplated `Style.ComboBox` gained a placeholder shown when `SelectedItem` is null, filled from the control's `Tag`; the window combo binds that to `WindowLabel`. FR-306 calls the series' own window an initial *preset*, but it is rarely one of the five FR-305 names - LIDC-IDRI-0001 arrives at W1600/L-600 against a Lung preset of W1500 - so the selection is legitimately empty and the box read blank. It now reads the numbers instead. The trigger tests `SelectedItem` rather than `SelectionBoxItem`, which is the empty string and not null when nothing is selected | Manual | Done |
| FR-410 | Each measurement carries a stable identifier, shown beside it and exported with it | `Measurement.Id`, assigned by `MainViewModel.AddMeasurement` from a counter that restarts when the list empties. Not in the original spec - added at the user's request, who asked which standard DICOM sets for this. It sets none: SR (TID 1500/300) and Presentation States standardise storage and exchange, carry an optional free-text label, and say nothing about what a viewer draws. A stable sequential number is convention. Stable rather than the list position, because deleting the second of three would renumber the third into its place and a CSV exported earlier would then name a different measurement than the same number does on screen. Init-only rather than positional, so identity is not part of the record's value and `with` carries the number through an edit | `MeasurementCsvTests` cover the new leading `id` column; `EveryRowHasAsManyFieldsAsTheHeader` is what catches a row left one column short | Done |
| FR-411 | Move an existing measurement and resize it by its endpoints | `ViewportControl.TryStartEdit` and `DragEdit`. Not in the original spec - added at the user's request. The press picks the endpoint within eight screen pixels or the whole shape otherwise, decided in screen pixels because the question is whether a hand landed on a handle, and a millimetre tolerance would be several handles wide zoomed out. The translation is the difference between two points read off the same plane, so it lies in that plane by construction and the frame FR-406 tests against needs no adjusting. Every move recomputes from the measurement as it was at the press, so a long drag accumulates nothing. The record is replaced in the list rather than mutated, and `with` carries the FR-410 identifier through the edit. Known limit: a region has handles on the two corners its creating drag produced, not on all four | `AnInPlaneMoveLeavesTheMeasurementOnItsOwnSlice`, `AMoveAlongTheNormalWouldNotBeSafeToLeaveTheFrameAloneFor` (which is what makes the first non-vacuous), `DraggingOneEndResizesAboutTheOtherAndKeepsTheIdentifier`. The gesture itself is manual | Done |
| FR-408 | Export the measurement list to CSV | `MeasurementCsv.Write` builds the document in Core and returns a string; the App layer only chooses a path, so the format is unit-tested and the handler has nothing in it but IO. Seventeen columns: kind, both endpoints and the frame's normal in patient millimetres, length, area, voxel count and the four statistics. The normal is what makes an oblique row reproducible - nothing else in the file says which plane the measurement was made on. A distance leaves the region columns empty rather than filling them with zeros. Invariant culture throughout, because a comma decimal separator splits a value across two columns on a European locale. A leading `#` disclaimer line is a deliberate addition, not a requirement: RQ-1 asks only for the banner and FR-409 only for the PNG | `MeasurementCsvTests` (5): `TheFileLeadsWithTheDisclaimerAndTheHeader`, `EveryRowHasAsManyFieldsAsTheHeader` (all three kinds plus a region entirely off the end of the data), `ADistanceCarriesItsLengthAndNoRegionColumns` (3-4-12, exactly 13 mm), `ARectangleCarriesItsAreaAndItsStatistics` (40 voxels of 40 HU, spread exactly zero, derived from the phantom), `NumbersUseTheInvariantDecimalPointWhateverTheMachineDoes` (run on a de-DE thread) | Done |

## Iteration 5

| ID | Requirement | Where it is satisfied | Evidence | Status |
|---|---|---|---|---|
| FR-102 | Prompt when a folder holds several series | `ISeriesPrompt`, implemented by `SeriesPrompt` over a modal `SeriesPromptWindow`. The load path became two background passes with a decision between them: the header-only scan has to finish before there is anything to choose from, the prompt has to run on the UI thread, and the expensive build must not start until the choice is made. An interface rather than the view model opening a window itself, so the load path stays testable and carries no WPF type. The list is largest first with the first row preselected, so pressing Open immediately reproduces the old silent behaviour. Cancelling leaves whatever was already loaded exactly as it was - an abandoned open is not a reason to clear the screen. DI-1: description, modality and slice count only, no identifiers | Manual | Done — supersedes the Iteration 1 partial row |
| FR-201 | Four viewports in a 2x2 layout: axial, coronal, sagittal, slab | `MainWindow.xaml`'s viewport grid holds four `ViewportControl`s over a one-pixel border brush, which is what draws the hairline gutters - there is no chrome between the panes to draw. FR-203's maximize stretches one pane over all four cells rather than re-parenting it, so it keeps its bitmap, its zoom and its handlers | Manual — visible in `docs/images/mpr-2x2.png` | Done — recorded late, in Iteration 5 |
| FR-308 | All viewport interactions maintain interactive frame rates per NFR-201 | Nothing allocates per frame: the `WriteableBitmap`, the pixel buffer and the window/level lookup table are reused, so a scroll or a window drag produces no collection. Renders are synchronous inside the gesture handler, which is affordable because the measured cost is two orders of magnitude inside the budget | `docs/performance.md`: 2.354 ms axial and 1.949 ms oblique against 8 ms and 16 ms; the slab at 14.1 ms against 33 ms after the Iteration 3 optimization | Done |
| NFR-201 | Axis-aligned slice render under 8 ms for 512x512 | `PlaneRenderer`, with the `AxialFastPath` special case for contiguous memory. The LUT turns per-pixel work into one array lookup | BenchmarkDotNet, committed in `docs/performance.md`: 0.105 ms on the fast path and 2.354 ms through the general renderer. Both pass; the viewports use the general path, so the honest number is 2.354 ms with 3.4x headroom | Done |
| NFR-204 | The UI thread never blocks for more than 50 ms during any interaction | Three things hold it: loading runs on the thread pool as two `Task.Run` passes with progress crossing back through `Progress<T>`; rendering is synchronous but measured at 1.9 to 14 ms; and nothing allocates per frame. Written up in `docs/architecture.md` under Threading | Manual, as the NFR itself specifies, with the render costs measured in `docs/performance.md` | Done |
| NFR-302 | Line coverage of Core and Rendering above 70% | Coverage is aimed at the domain and rendering layers, where a defect is silent, and the view layer is deliberately left to manual verification | `dotnet test --collect:"XPlat Code Coverage"`: **InterviewTrea.Core 96.5%**, **InterviewTrea.Rendering 93.2%** line rate. Both well past the 70% bar | Done |
| FR-409 | Export a chosen viewport, or the whole 2x2 layout as displayed, as a PNG with the RQ-1 disclaimer burned in | `ViewportCapture.Render` over the pane's own visual tree, or over the viewport grid for the whole layout - gutters included, so the file is the layout someone was looking at rather than four pictures to reassemble. What is captured is the visual tree rather than a re-render from the volume, so the export is provably what was on screen. The caption goes into the pixels over the bottom of the image rather than below it, so the file keeps the pane's aspect ratio and removing the statement means visibly cropping the picture. Captured at device pixels, because on a 150% display layout pixels give a visibly soft export of fine greyscale detail. The dropdown is the button: it names itself while closed and lists what it can capture when opened, so choosing a target and running the export are one gesture rather than a setting and then a command. It clears back to its own name afterwards, because a control that is an action should not look like one holding a pending choice | Manual | Done |
| FR-412 | Zoom shall not go below fit, and pan shall not move the image out of its viewport | `ViewportControl.ClampUser`, applied where the view matrix is composed rather than in the gesture handlers, so it holds for zoom, pan, a window resize and a new series alike. Zoom floors at fit because zooming further out only adds black and eventually loses the image entirely, with no way back except the control that exists to undo it. Pan is bounded per axis: an image larger than the pane may not bring its edges inside, so there is always anatomy under the cursor, and one smaller than the pane on that axis is centred. `Reset` (FR-310) now returns zoom and pan to fit as well, reversing the earlier choice to leave them - a pane left magnified after a reset was the one pane still showing what the user asked to undo | Manual | Done |
| FR-501 to FR-507 | *(removed)* | The clinical application platform and its reference application were built in Iteration 5 and deleted when the project scope settled on the viewer plus a 3D view. Nothing was left for the seam to host, and Phase 1 §1.6 says a feature you cannot account for is deleted rather than kept. See [ADR-004](decisions/ADR-004.md) for the decision and its reversal | n/a | Removed |
| RQ-1 (reuse) | One disclaimer string | `Core.Disclaimer.Text` is RQ-1's sentence character for character; the banner binds it through `x:Static`, the PNG caption draws it, and the CSV writes `Disclaimer.Ascii`, which is the same string with the em dash flattened and is derived from it rather than written out again. A CSV is read by machines: UTF-8 with no byte-order mark opened in a spreadsheet on a legacy code page turns that one character into mojibake on the one line whose whole purpose is to be read | `MeasurementCsvTests.TheFileLeadsWithTheDisclaimerAndTheHeader` | Done |

## Phase 2 — Iteration 6

Rows for `INTERVIEWTREA-PHASE2-3D-VIEWER.md`. Same rule: added in the change that
satisfies the requirement.

| ID | Requirement (short) | Design element | Test(s) | Status |
|---|---|---|---|---|
| FR-608 (camera) | Orbit, zoom and pan an orthographic camera | `Camera3D`, holding a target, azimuth, elevation and a view height in millimetres - not a position and not an accumulated matrix, for the same reason the Phase 1 reslice frames hold axes rather than a rotation history. Spec 4.1 also lists an eye distance and there is none: under a parallel projection every ray has the same direction, so moving the eye only slides each ray along itself and changes no pixel. Zoom is therefore the view height, which is the only thing that does change the scale. Elevation is clamped a degree short of the pole, where superior is parallel to the view and the up vector is undefined. The frontal view puts the patient's left on the right of the image, which is radiological convention | `Camera3DTests` (10): the frontal view looks along +Y and letters left-right correctly; the three image axes stay orthonormal at every orbit angle including past the clamp; adjacent pixels are exactly one pitch apart and the four central pixels of an even image average to the target; panning moves within the image plane and not at all along the view; framing spans the body diagonal | Partial — the gestures that drive it are Iteration 7 |
| FR-602 (entry/exit) | Ray casting with trilinear sampling and front-to-back compositing | `RayBox.TryIntersect` - the three-axis slab test, run in voxel space where the volume is axis-aligned however the patient geometry is oriented. An affine map preserves the ray parameter, so the t values come back as the patient millimetres that went in, which is what makes the chord through an anisotropic volume 120 mm rather than 40 voxel steps. It intersects a line and not a half-line: the orthographic camera puts every ray origin on a plane through the middle of the volume, so a negative entry is normal and clamping it to zero would render only the far half of the patient. The domain is [0, Dim-1] to match `Volume.ContainsContinuous`; if it did not, the silhouette would carry a one-voxel skin the sampler refuses to fill | `RayBoxTests` (6): an axis ray enters and exits at the exact millimetre; a ray starting inside enters at a negative parameter; a ray alongside misses; a ray exactly parallel to two faces and outside them misses without dividing by zero; a corner-to-corner ray measures 40*sqrt(3); anisotropic spacing comes back in millimetres | Partial — the march that consumes it is next |
| FR-604 | Classification by an editable transfer function, held as a lookup table over -1024..3071 | `TransferFunction`. One entry per Hounsfield unit, 4096 of them, so there is no quantisation to argue about; the same decision as the Phase 1 window/level LUT and for the same reason - classification runs once per sample and a ray takes hundreds of samples, so per-sample cost has to be one array read. Colour and opacity interpolate linearly between control points and clamp outside the outermost ones rather than extrapolating, which would run opacity past 1 within a few hundred units the moment someone dragged the lowest point up. Points must strictly increase: two at the same value ask for a vertical step and the interpolation would divide by zero drawing it. Colours are exposed as a `ReadOnlySpan<byte>` rather than the array, so the table cannot be written through from outside | `TransferFunctionTests` (11): a control point maps to exactly its own colour; the midpoint is the exact average, including the rounding that keeps every ramp from sitting half a level dark; outside values clamp; a three-segment function interpolates in the right segment at every value, which is what catches the forward-walking table build stepping too far | Done |
| FR-603 | Sample opacity corrected for step size | `TransferFunction.OpacitiesForStep`. Opacity is quoted per millimetre of ray, so halving the step doubles the number of samples through the same tissue and, uncorrected, doubles what it stops. Transparency compounds, so the correction is `1 - (1 - A)^(step / 1 mm)`. Without it FR-609's low-resolution preview would not be a coarser version of the final image but a different picture. Fully transparent and fully opaque entries are passed through rather than sent through `Pow`, which is both the common case in a preset and the only way an opaque surface stays exactly opaque | `HalvingTheStepHalvesWhatEachSampleStopsInTheCompoundingSense` - two half-millimetre samples must stop exactly what one whole-millimetre sample stops, so the corrected value is `1 - sqrt(0.5)` derived on paper, and the test then composites the two and checks it lands back on 0.5. Plus the identity at the reference step and the two fixed points | Done |
| FR-605 | Named presets: Bone, Angio, Lung, Skin | `TransferFunctionPreset`. These are data, not arithmetic - the thresholds are CT tissue ranges and the colours are tuned by eye, which is why FR-606 makes them a starting point rather than a fixed set. Every preset opens with a zero-opacity point at the bottom of the scale, because everything below the lowest point clamps up to it and that point is what makes air invisible | `EveryPresetIsAValidTableThatLeavesAirInvisible` over all four. It found a real defect on the first run: Lung started its ramp at -950 with opacity 0.008, which puts 0.0026 per millimetre on scanned air at -1000 and accumulates to roughly 65% over the 400 mm of air a ray crosses outside the patient - a fog around the whole chest. The ramp now starts at -980 | Done |
| FR-601 | Render a volume-rendered 3D view of the loaded volume | `VolumeRaycaster.Render`, over the same `Volume` the MPR views read - no second copy and no resample. Returns BGRA32 into a caller-supplied `byte[]`, exactly as the 2D renderer returns Gray8, so the WPF layer's only job is to wrap it. A `byte[]` rather than a `Span<byte>` because the scanlines run in parallel and a ref struct cannot be captured; the 2D renderer takes a span because it is single-threaded | `VolumeRaycasterTests` (11) | Done — the view that shows it is Iteration 7 |
| FR-602 | Ray casting, trilinear sampling, front-to-back over, early termination at 0.99 | The march is `VolumeRaycaster.Cast`; the operator is `RayAccumulator`, separated out because it is the one piece of arithmetic here that can be checked on paper without rendering anything. Front to back rather than back to front is what makes termination possible at all: accumulated opacity only grows, so once it saturates nothing further along the ray can change the pixel. Sampling is at segment midpoints, not flush against the entry face, which would give the first voxel a full sample's weight for half a step of ray and render a cube half a step too wide. The transfer function's direction converts to voxel space once for the whole image, because under a parallel projection every ray shares it | `RayAccumulatorTests` (5): two half-opaque samples stop 0.75 and not 1.0; front-to-back and back-to-front agree on the same four samples, which is what proves the accumulation. `EarlyTerminationDoesNotChangeThePicture` renders the same phantom at 0.99 and at 1.0 and compares all 16384 pixels within three levels - the optimization is only allowed if it is invisible | Done |
| FR-603 (end to end) | Changing the step changes resolution, not appearance | `VolumeRaycaster.Render` corrects the opacity table once per render rather than once per sample - 4096 `Pow` calls against one per sample | `HalvingTheStepLeavesTheImageAloneBecauseOpacityIsCorrectedForIt` (1.0 mm against 0.25 mm, within three levels) and its converse `WithoutTheCorrectionTheSameChangeOfStepWouldChangeTheImage`, which accumulates the raw table by hand at both rates and shows they differ by far more. Without the second half the first would pass on a renderer that ignored the step entirely | Done |
| FR-602 (geometry) | The picture is in the right place and the right size | `AUniformCubeRendersARectangleOfTheSizeTheGeometryPredicts`: a 40 mm cube at one pixel per millimetre lights between 39x39 and 41x41 pixels, the one pixel of latitude being where the trilinear ramp crosses the classification threshold, a quarter of a millimetre outside the last solid voxel. `ACentreRayAccumulatesWhatFortyMillimetresOfTissueShouldStop` pins the value as well as the extent: 5% per millimetre over 40 mm is `1 - 0.95^40 = 0.8715`, so the centre pixel is 222 of 255. `AnAnisotropicVolumeIsRenderedInMillimetresAndNotInVoxels` renders the same cube through 0.7 x 0.7 x 3.0 mm voxels, where it is 57 voxels wide and 13 tall and still has to come out square. `TheCubeIsTheSameSizeFromAnySideOfAnIsotropicVolume` is the parallel projection: a perspective one would fail it | Done |
| NFR-401 (groundwork) | Interactive frame rates for the 3D view | `Parallel.For` over scanlines - a row is enough work to cover scheduling and each row writes a disjoint run of the buffer, so nothing is shared and nothing locks. Early termination is where most of the saving is: with a surface preset the majority of rays stop within a few millimetres of the skin | Not yet measured. Benchmarks are Iteration 7, and the number goes in `docs/performance.md` measured rather than claimed | Partial |
| FR-607 | Gradient-based Phong shading, normals from central differences, toggleable | `GradientShader`, switched by `RaycastSettings.IsShaded`. The gradient of the density field is the surface normal wherever there is a surface, by central differences one voxel either side; a forward difference would be half the samples and would bias every normal half a voxel toward the front of its surface. The division is by patient millimetres and not by voxel indices, which is the anisotropy trap in three dimensions: on 0.7 x 0.7 x 3.0 mm voxels the same physical slope reads four times steeper along z and every surface tilts toward the axial plane. A single headlight at the eye collapses the Blinn half-vector onto the light direction, so one dot product serves both the diffuse and the specular term. The normal is taken as facing the viewer whichever way the gradient runs, because its sign says whether the ray reached tissue from air or air from tissue - a property of the ray, not of the surface. Below `MinimumGradient` the field is flat and the sample is returned unshaded rather than normalised. Samples the transfer function already made nearly invisible are not shaded at all, since shading costs about four times a plain sample | `GradientShaderTests` (8): a 100 HU per voxel ramp gives exactly 100 HU/mm along x and exactly zero on the other two axes at every interior voxel; the same ramp at 0.7 mm gives 142.857, which is what catches dividing by index; a uniform volume has no gradient and shading returns 1 rather than a NaN; a surface facing the viewer is lit by every term and one edge-on falls back to ambient exactly; a surface is lit the same from either side; and end to end, a shaded sphere is bright in the middle and dim at the rim where the unshaded control is a flat disc | Done |
| FR-607 (revised) | The gradient offset is one physical distance, not one index step per axis | `GradientShader.Gradient` takes its central difference over one voxel of the *coarsest* axis on all three axes. Both forms give the same answer on a linear ramp, so this is not correctness in the analytic sense - it is what the estimate is averaged over. One index step per axis smooths the normal over 0.7 mm across a slice and 3.0 mm through the stack, four times harder in one direction than the others, and on a real chest study that reads as horizontal banding across the whole rendering. Found by rendering LIDC-IDRI-0001 and looking at it, not by a test; the flat render was clean and the shaded one banded, which is what identified the gradient rather than the compositing. It costs nothing - the same six trilinear samples, taken further apart on the fine axes | `TheDifferenceIsTakenOverTheSamePhysicalDistanceOnEveryAxis`: 2 mm outside a 6 mm cube on 0.7 mm voxels, an isotropic 3 mm reach still finds the edge where one index step sees only air, with the converse at 5.5 mm out so it cannot pass by reaching everywhere | Done |
| FR-605 (revised) | The presets are tuned against a real study, not only against phantoms | Lung and Angio were both wrong on LIDC-IDRI-0001 in ways no phantom could show. Lung left soft tissue at 0.05 per millimetre, which is a solid grey block across 200 mm of chest with the lungs invisible behind it; it is now a window of -950 to -500 with everything denser fully transparent, so the lungs are seen through the body rather than behind it. Angio ran red to 900 HU at high opacity and painted the whole skeleton red on a non-contrast study. The red is now confined to 200-500 and gives way to bone white by 700. Angio still tints the rising edge of every bone, and that is documented rather than tuned away: a transfer function classifies by density, the outside of a rib passes through 300 HU, and telling that from a vessel at 300 HU is segmentation, which Phase 2 section 1.4 rules out | Manual, against a real chest study. The automated test is the invariant one - every preset leaves air at -1000 exactly invisible | Done |
| FR-608 | Left-drag orbits, wheel zooms, middle-drag pans, orthographic projection | `VolumeViewControl`'s three handlers over `Camera3D`. Orbit inverts the horizontal drag, because dragging right should turn the volume right, which means walking the camera the other way round it. Pan converts pixels to millimetres through the current view height, so it tracks the cursor at any zoom instead of accelerating as the view narrows. Zoom is multiplicative and clamped to 10..2000 mm of view height - the 3D form of FR-412, and for the same reason: neither end leaves anything on screen worth looking at. There is no fit-zoom-pan matrix as there is on an MPR pane, because zoom is the camera's view height and pan is its target, so both happen in patient space before a ray is cast rather than to the pixels afterwards. That is why zooming the 3D view resolves more detail where zooming an MPR pane only magnifies what was already rendered | Manual. The camera arithmetic underneath is `Camera3DTests` | Done |
| FR-609 | Progressive refinement: reduced resolution while moving, full resolution when it stops | `VolumeViewControl`. Every camera change puts up a half-resolution, quadruple-step, unshaded frame synchronously and restarts a 180 ms timer; when the timer fires, the full frame runs on the thread pool and is written back on the UI thread. About a sixteenth of the work, measured at 11 ms against 210 ms on the reference study. Shading is off in the preview whatever the checkbox says, because it is six extra trilinear samples per step and a frame that arrives late is worse than one that arrives flat. The full frame is cancelled through the `Parallel.For`'s own token when the camera moves again, rather than being waited for and thrown away. The preview looks like the frame it resolves to only because FR-603's correction follows the step - without it this would be the classic preview that resolves into a different picture | Manual, with the two timings measured on LIDC-IDRI-0001 and recorded in `docs/performance.md` | Done |
| FR-610 (revised) | The 3D view is selectable, and exportable as a PNG under FR-409 | The 3D view is a **mode of the fourth pane**, not a layout: the dropdown that already chooses between MIP, MinIP and Average gains a "3D" entry. The spec asked for a layout selector; that would have been a new visible control, and Phase 1 section 1.6 says every one of those has to earn its place. This one could not - the 3D view answers the same kind of question the slab projections answer, so the control that already decides that keeps deciding it. See [ADR-006](decisions/ADR-006.md). The PNG export needed one change: the fourth entry names a cell that two controls share, so it captures whichever is actually on screen, because exporting the hidden one would write out a stale bitmap | Manual | Done |
| FR-611 | The 3D view shows no measurement or Hounsfield readout (RQ-5) | `VolumeViewControl.xaml` has no crosshair, no position readout, no hover value and no measurement canvas - not hidden ones, absent ones. What the overlay does carry is the preset name and the render size and step, because which classification produced the picture is exactly what a reader needs in order to interpret it. The measurement tools are unaffected and still work in the three MPR panes, where a value is a fact about one plane | Manual - the control has no code path that could draw one | Done |
| FR-612 | With no volume loaded the 3D view shows the same calm empty state as the MPR panes | The camera is null until a volume is loaded, and the control clears its image and returns. What is left is the viewport background inside the pane's own border - no error, no half-drawn frame, and the same shell status banner covers the whole grid as it does today | Manual | Done |
| NFR-401 | A full-resolution 512x512 shaded frame under 400 ms | Scanline `Parallel.For`, early termination at 0.99, and no shading of samples the transfer function already made nearly invisible | Measured on LIDC-IDRI-0001 (512x512x133, 0.7 x 0.7 x 2.5 mm) at 512x512 with shading: Bone 210 ms, Angio 217 ms, Lung 214 ms, Skin 85 ms. All inside 400 ms | Done |
| NFR-402 | An interaction frame under 50 ms | Half resolution, quadruple step, no shading | Measured at 11 ms on the same study - 256x256 at 1.4 mm. Nearly five times inside the budget, which is what makes an orbit continuous rather than merely responsive | Done |
| NFR-405 | `InterviewTrea.Rendering3D` does not reference `System.Windows.*` | `Directory.Build.props`: the project targets plain `net8.0`, so the WPF types are not in the compilation. The WPF layer wraps the `byte[]` in a `WriteableBitmap` and the renderer never learns that happened | Manual, same as NFR-301 | Done |
| NFR-403 | Rendering allocates nothing per frame | The output buffer, the bitmap and the corrected opacity table are made once per size and reused. There are two bitmaps rather than one, because the preview and full frames are different sizes and alternate every time a gesture is followed by a settle - one would have been reallocated on every frame of a drag. The opacity correction is 4096 `Pow` calls once per render rather than one per sample, which is about five orders of magnitude fewer | Not yet asserted by a benchmark's allocation column. Iteration 6 and 7 measured time, not allocation, and the honest status is that this is reasoned rather than measured | Partial |
| NFR-406 | Line coverage of `InterviewTrea.Rendering3D` above 70% | Coverage is aimed at the renderer, where a defect is silent; the 3D view control is left to manual verification like every other view | `dotnet test --collect:"XPlat Code Coverage"`: **InterviewTrea.Rendering3D 98.1%** line rate. Core is 96.5% and Rendering 93.2%, both unchanged | Done |
| RQ-5 | A volume rendering shall never be presented in a way that implies measurement | Enforced by absence rather than by a check: `VolumeViewControl` has no crosshair, no position readout, no hover value and no measurement canvas, so there is no code path that could draw one. The overlay carries the preset name and the render size and step instead, because which classification produced the picture is what a reader needs to interpret it. The RQ-1 banner and the burned-in PNG caption apply to the 3D view exactly as to the others | Manual | Done |
| FR-201 (revised) | Four viewports: axial, coronal, sagittal, and the 3D view | The fourth pane is the volume rendering, permanently. It used to be a slab projection that the 3D view could displace through a dropdown, which made the two things a reader most wants side by side mutually exclusive. See [ADR-006](decisions/ADR-006.md) | Manual | Done |
| FR-207 (revised) | MIP, MinIP and Average now project through the three planar panes | A slab was always a property of a plane rather than a pane: "collapse n millimetres either side of *this* plane". The old fourth pane was literally `new ViewportViewModel(Axial, isSlab: true)` - a duplicate of pane one, existing only to have something to project through. Deleting it removes the duplicate and gives the three panes a coronal and sagittal MIP, which the old design could not produce at all. Thickness starts at **off**, where `ViewportControl` calls `PlaneRenderer` exactly as before, so the viewer opens on the same three single planes it always did; Shift+wheel over any pane opens the slab, and the pane's own overlay reads e.g. `MIP 20 mm`. Zero is a rung of its own below the geometric 1-100 mm range, because multiplying cannot reach it and a gesture that cannot be reversed is worse than one that is missing | Manual. `SlabRenderer`'s own phantom tests are unchanged - the renderer did not move, only what calls it | Done |
| FR-207 / FR-405 | While a slab is open, no pane offers a Hounsfield readout or a measurement | One rule, `MainViewModel.IsProjecting`, replacing the per-pane `IsSlab` flag it used to be. A projected pixel is the brightest (or dimmest, or mean) sample somewhere within the thickness and has no one depth, so a value labelled with the crosshair's position would be a value about a different point, and a distance drawn on a MIP joins two structures that may be centimetres apart through the slab. The old design enforced this by having one pane that could not measure; this enforces it by having a state in which none can, which is the same rule said once instead of four times. Scrolling the thickness back to zero returns everything | Manual | Done |
| FR-607 (revised) | Shading is always on, not toggleable | The spec asked for a toggle. An unshaded volume rendering is a picture nobody would choose - it is the same compositing with the surface cue removed - so the checkbox had a right answer and a wrong one, which is a default rather than a choice, and a visible control that only ever gets set one way is exactly what Phase 1 section 1.6 exists to catch. The preview frame still renders unshaded, because that is a cost decision inside FR-609 rather than a setting | Manual | Done |
| FR-409 (revised) | The PNG export's fourth target is the 3D pane | `ExportTarget.IsVolume`. The target used to name a `ViewportViewModel`; the 3D pane has none, because it has no reslice plane. A null viewport already meant "the whole grid", so the flag names the third case rather than overloading the null | Manual | Done |
| FR-613 | One clip plane on the 3D view, trimming in from the patient's back | `VolumeClip`. The scanner table renders as a flat slab behind the patient, and it cannot be removed by classification: a CT couch reads 0 to 100 HU, which is where soft tissue reads, so any transfer function that hides the table hides the patient. Segmentation is a stated non-goal, so the remaining separation is geometric - the table is posterior to the patient and nothing else is. The axis is not a guess: DICOM patient coordinates are LPS, so +y **is** posterior by definition, which is why the clip is expressed in patient millimetres rather than in voxel indices - an obliquely acquired study has no voxel axis pointing at the patient's back but still has a +y. The depth is measured inwards from the volume's own posterior face so the same number means the same thing on the next study, and it is applied by narrowing the ray interval rather than by testing each sample, which is two compares per ray instead of one per step. Shift+wheel over the pane, 10 mm a notch, reported in the overlay - no new visible control | `VolumeClipTests`: the extent of a 65-voxel 1 mm box is -32 to +32 by construction; the three narrowing cases have hand-computed t values, including the anterior-travelling ray where it is the *entry* that moves - getting that sign backwards would keep the table and throw away the patient, so it is pinned rather than assumed. End to end, a 40 mm cube viewed from the side at one pixel per millimetre is 40 columns wide, is still 40 wide after a 10 mm clip that reaches only air, and is 30 wide after a 22 mm clip that reaches 10 mm into it | Done |
